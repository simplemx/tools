#! /usr/bin/env python
#coding=gbk

import os

path =  "D:\\git\\tools\\javaeye_blog_backup"
output_path = "D:\\output"

def format(path, files):
	for file in files:
		if file
def formatFiles():
	for (p,d,files) in os.walk(path):
		format(p, files)

if __name__ == "__main__":
	formatFiles()
